# CSS\+\+

Work in progress CSS to JS compiler built because I thought it'd be fun

Keywords
============
state id = value => let id = value
on *event* => *element*.addEventListener('event')
when value1 condition value2 => if (value1 condition value2)
